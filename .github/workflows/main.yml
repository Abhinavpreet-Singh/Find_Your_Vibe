name: 🚀 Find Your Vibe CI Workflow

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-lint:
    name: 🧪 Lint & Build Vite React App
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout Repository
        uses: actions/checkout@v3

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 📥 Install Dependencies
        run: npm install

      - name: 🧹 Lint Code
        run: npm run lint

      - name: 🏗️ Build Project
        run: npm run build
